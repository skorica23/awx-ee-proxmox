version: 3

images:
  base_image:
    name: quay.io/ansible/awx-ee:latest  #ansible-runner:stable-2.12-latest

dependencies:
  ansible_core:
    package_pip: ansible-core==2.15.1

  # ansible_runner:
  #   package_pip: ansible-runner
   
  galaxy:
    collections:
      - name: community.proxmox
        version: ">=1.4.0"

# additional_build_steps:
#   prepend_builder:
#     - RUN python3 -m pip install --upgrade pip setuptools wheel
  
